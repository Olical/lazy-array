{
  "name": "lazy-array",
  "version": "1.0.0",
  "description": "Lazy array implementation, sort of like Clojure's lazy-seq.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^2.2.5"
  }
}
